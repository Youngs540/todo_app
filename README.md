# todo_app

Minimal command-line todo app that persists tasks to a JSON file.

Overview
- Simple CLI for adding, viewing, marking done, and deleting tasks.
- Tasks are stored in `todo.json` so data persists between runs.

Features
- Add, view, complete, and delete tasks.
- Safe persistence with JSON; handles missing or empty data files.

